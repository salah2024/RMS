
<style>
    /* نوار کناری */
    .sidebar {
        position: fixed;
        top: 56px;
        right: 0;
        height: calc(100% - 56px);
        width: 250px;
        background-color: #2c3e50; /* رنگ سرمه‌ای */
        color: white;
        padding-top: 20px;
        transition: all 0.3s ease;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

        /* لینک‌های نوار کناری */
        .sidebar a {
            display: flex;
            align-items: center;
            color: white;
            padding: 15px;
            text-decoration: none;
            border-bottom: 1px solid #34495e; /* رنگ کمی روشن‌تر */
            transition: background-color 0.3s ease;
        }

            /* آیکن ها */
            .sidebar a i {
                margin-right: 10px; /* فاصله بین آیکن و متن */
                font-size: 18px;
            }

            /* افکت هاور */
            .sidebar a:hover {
                background-color: #4b6a8b; /* رنگ هاور برای لینک‌ها (سرمه‌ای کمرنگ) */
            }

        /* منوهای باز شونده */
        .sidebar .dropdown-toggle {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px;
            color: white;
            font-weight: bold;
            border: none;
            background: none;
            cursor: pointer;
        }

        /* زیر منوها */
        .sidebar .submenu {
            padding: 12px 30px;
            color: #95a5a6; /* کمرنگ‌تر کردن رنگ متن زیرمنو */
            text-decoration: none;
            border-bottom: 1px solid #7f8c8d; /* رنگ خاکی کمرنگ */
            background-color: #34495e; /* رنگ پس‌زمینه کمرنگ‌تر */
            transition: background-color 0.3s ease;
        }

            /* افکت هاور زیر منو */
            .sidebar .submenu:hover {
                background-color: #4b6a8b; /* رنگ سرمه‌ای کمرنگ برای رنگ هاور */
                color: white; /* تغییر رنگ متن به سفید در حالت هاور */
            }

        /* انیمیشن برای منوهای باز شونده */
        .sidebar .collapse {
            transition: max-height 0.5s ease;
            max-height: 0;
            overflow: hidden;
        }

            .sidebar .collapse.show {
                max-height: 200px;
            }
</style>

<div class="sidebar">
    <a href="#">
        <i class="fas fa-home"></i>
        خانه
    </a>
    <!-- منوی باز شونده برآورد -->
    <div class="dropdown">
        <a href="#" class="dropdown-toggle" data-bs-toggle="collapse" data-bs-target="#dastyarMenu" aria-expanded="false">
            <i class="fas fa-user"></i>
            برآورد
        </a>
        <div class="collapse" id="dastyarMenu">
            <a href="@Url.Action("ShowTree", "Operation")" class="submenu">دستیار</a>
        </div>
    </div>

    <!-- منوی باز شونده پروفایل -->
    <div class="dropdown">
        <a href="#" class="dropdown-toggle" data-bs-toggle="collapse" data-bs-target="#profileMenu" aria-expanded="false">
            <i class="fas fa-user"></i>
            پروفایل
        </a>
        <div class="collapse" id="profileMenu">
            <a href="#" class="submenu">نمایش پروفایل</a>
            <a href="#" class="submenu">ویرایش پروفایل</a>
        </div>
    </div>

    <!-- منوی باز شونده تنظیمات -->
    <div class="dropdown">
        <a href="#" class="dropdown-toggle" data-bs-toggle="collapse" data-bs-target="#settingsMenu" aria-expanded="false">
            <i class="fas fa-cogs"></i>
            تنظیمات
        </a>
        <div class="collapse" id="settingsMenu">
            <a href="#" class="submenu">تنظیمات حساب</a>
            <a href="#" class="submenu">تنظیمات امنیتی</a>
        </div>
    </div>

    <!-- منوی گزارشات -->
    <a href="#">گزارشات</a>
    <a href="#">خروج</a>
</div>
